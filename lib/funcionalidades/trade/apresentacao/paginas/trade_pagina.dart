import 'package:flutter/material.dart';

class TradePagina extends StatelessWidget {
  const TradePagina({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Trade'));
  }
}
